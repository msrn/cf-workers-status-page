settings:
  title: 'Status'
  url: 'https://statuswasd.fi' # used for Slack messages
  logo: logo-192x192.png # image in ./public/ folder
  daysInHistogram: 90 # number of days you want to display in histogram
  collectResponseTimes: true # collects avg response times from CRON locations

  allmonitorsOperational: 'Kaikki toimii!'
  notAllmonitorsOperational: 'Kaikki ei toimi :/'
  monitorLabelOperational: 'Pystyssä'
  monitorLabelNotOperational: 'Ei pystyssä'
  monitorLabelNoData: 'Ei tietoa'
  dayInHistogramNoData: 'Ei tietoa'
  dayInHistogramOperational: 'Makeimmillaan
  dayInHistogramNotOperational: ' incident(s)' # xx incident(s) recorded

monitors:
  - id: wasd
    name: wasd.fi
    description: 'You write code. They handle the rest.' # default=empty
    url: 'https://wasd.fi' # URL to fetch
    method: GET # default=GET
    expectStatus: 200 # operational status, default=200
    followRedirect: false # should fetch follow redirects, default=false
    linkable: false # allows the title to be a link, default=true
  - id: grafana
    name: Grafana
    url: 'https://grafana.wasd.fi' # URL to fetch
    method: GET # default=GET
    expectStatus: 200 # operational status, default=200
    followRedirect: true # should fetch follow redirects, default=false
    linkable: false # allows the title to be a link, default=true
  - id: nextcloud
    name: Nextcloud
    url: 'https://nextcloud.wasd.fi' # URL to fetch
    method: GET # default=GET
    expectStatus: 200 # operational status, default=200
    followRedirect: true # should fetch follow redirects, default=false
    linkable: false # allows the title to be a link, default=true
  - id: plex
    name: Plex
    url: 'https://plex.wasd.fi' # URL to fetch
    method: GET # default=GET
    expectStatus: 401 # operational status, default=200
    followRedirect: true # should fetch follow redirects, default=false
    linkable: false # allows the title to be a link, default=true
  - id: vault
    name: Vault
    url: 'https://vault.wasd.fi' # URL to fetch
    method: GET # default=GET
    expectStatus: 200 # operational status, default=200
    followRedirect: true # should fetch follow redirects, default=false
    linkable: false # allows the title to be a link, default=true

  - id: wallabag
    name: Wallabag
    url: 'https://wallabag.wasd.fi' # URL to fetch
    method: GET # default=GET
    expectStatus: 200 # operational status, default=200
    followRedirect: true # should fetch follow redirects, default=false
    linkable: false # allows the title to be a link, default=true
